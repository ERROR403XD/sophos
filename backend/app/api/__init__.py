"""API 路由层（M2 起）。

规划路由模块（挂载于 /api，逐端点定义见 docs/API_DESIGN.md）：
- health（M2）        workdirs（M2）      scan（M2）       jobs（M2）
- videos（M2/M4）     stream（M2）        faces（M3）      thumbs（M3）
- ratings（M5）       train（M6）
"""
