# Sophos 后端依赖（按批次分段；注释掉的段落到对应批次再启用）
# Python >= 3.10（宿主机实测 3.13.15）
# 国内网络可加: -i https://pypi.tuna.tsinghua.edu.cn/simple

# ===== M2: Web 框架与数据库 =====
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
httpx>=0.27            # fastapi TestClient 依赖
pytest>=8.0

# ===== M3: 人脸引擎 =====
# 注：宿主 Python 3.13 无 numpy 1.26 wheel（仅到 cp312），须用 numpy>=2.1
#     + onnxruntime>=1.20（二者均官方支持 3.13 与 numpy 2）
onnxruntime>=1.20
opencv-python-headless>=4.11
numpy>=2.1,<3
pillow>=10
# ffmpeg 为系统依赖（抽帧用），Docker 镜像内置；宿主机自行安装（tools/ 有绿色版脚本方案）

# ===== M6: 训练（定稿：numpy 手写联合线性头，零新增依赖） =====
# 原计划 sklearn Ridge/MLP；因 pairwise 联合训练需自定义损失（ADR-008/013），
# 改为 numpy 全批梯度下降（services/personalizer.py），不引入 sklearn/joblib
# 可选(后续非线性模型): torch —— 暂不引入

# ===== 转换期专用（运行时不依赖，已装在本机 venv，勿加入运行时 requirements） =====
# torch 2.14.0+cpu / torchvision / onnx / onnxscript  → 仅 scripts/convert_beauty_model.py 用
# transformers 5.x → 仅 scripts/export_clip_gender.py 用（R2 CLIP 第二意见导出，产物 clip_vitb32_*.onnx/.npz）
# gdown → 下载 Google Drive 文件用（本项目最终未用上）
# 运行时打分走 onnxruntime（上面 M3 段已含）
